<div class="container">
  <h1><%= @article.title %></h1>
  <%= link_to "Edit", edit_article_path(params[:id]) %>
  <%= link_to "Delete", article_path(params[:id]), method: :delete, data: {confirm: "Are you sure?"} %>
  <p><%= @article.content %></p>
</div>

